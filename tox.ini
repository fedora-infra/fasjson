[tox]
envlist = checks,docs,{py310,py311,py312,py313,py314}-unit
isolated_build = true

[testenv]
passenv = HOME
sitepackages = false
skip_install = true
allowlist_externals =
    poetry
commands_pre =
    poetry install
commands =
    unit: poetry run pytest -vv --cov --cov-report=html --cov-report=xml --cov-report=term-missing tests/unit {posargs}

[testenv:docs]
changedir = docs
deps =
    -rdocs/requirements.txt
allowlist_externals =
    {[testenv]allowlist_externals}
    rm
    mkdir
commands=
    rm -rf _build
    rm -rf _source
    rm -rf _api
    mkdir _api
    poetry run sphinx-build -W -b html -d {envtmpdir}/doctrees .  _build/html

[testenv:checks]
allowlist_externals =
    {[testenv]allowlist_externals}
    pre-commit
    git
commands =
    pre-commit run --all-files


# We're using Ruff now, but we leave this line in place for contributors whose
# editor still only runs flake8.
[flake8]
max-line-length = 100
